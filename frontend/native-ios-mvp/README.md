# Native iOS MVP (SwiftUI)

This folder holds a placeholder SwiftUI Xcode project that previously lived under
`frontend/ios/`, which broke `flutter build ios` because Flutter requires a single
`.xcodeproj` in the `ios/` directory.

The Flutter iOS app remains in `frontend/ios/` (Runner).
